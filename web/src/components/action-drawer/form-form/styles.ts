import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  fieldset: {
    width: "100%",
  },
  centerAlign: {
    display: "flex",
    alignItems: "center",
  },
});

export default useStyles;
